import { InjectionToken } from '@angular/core';

// interfaces
import { ICoreEnvironment } from '../../interfaces';

export const CORE_ENVIRONMENT = new InjectionToken<ICoreEnvironment>('CORE_ENVIRONMENT');
